# F1 by 324.ing — 작업 로그 (2026-03-08)

## 1. 역사 페이지 Table 컴포넌트 적용

**파일**: `src/app/history/page.tsx`

shadcn 패턴 `Table` 컴포넌트를 역사 페이지의 두 테이블에 적용.

- 역대 챔피언 (2000–2024) 테이블
- 역대 챔피언 (1950–1999) 테이블

기존 raw `<table>/<thead>/<tbody>/<tr>/<th>/<td>` → `Table / TableHeader / TableBody / TableRow / TableHead / TableCell / TableHeaderRow` 교체.

```ts
import { Table, TableHeader, TableBody, TableRow, TableHead, TableCell, TableHeaderRow } from "@/components/ui/Table";
```

---

## 2. 게시글 삭제 오류 수정

**파일**:
- `src/app/api/posts/[id]/route.ts` — `DELETE` 핸들러 추가
- `src/components/community/PostEditActions.tsx` — API 호출 방식 변경

### 원인
`deletePost()`가 클라이언트 Firebase SDK(`deleteDoc`)를 직접 호출 → Firestore 보안 규칙에서 차단.
어드민 인증은 쿠키(`pitlane_admin`) 기반이라 Firebase Auth와 무관.

### 해결
서버 API 엔드포인트(`DELETE /api/posts/[id]`)를 신설하여:
1. `pitlane_admin` 쿠키 검증
2. Firebase Admin SDK로 삭제 (보안 규칙 우회)

```ts
// route.ts — DELETE 핸들러
export async function DELETE(_req, { params }) {
  const cookieStore = await cookies();
  if (cookieStore.get("pitlane_admin")?.value !== "authenticated") {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
  }
  const { id } = await params;
  await getAdminDb().collection("posts").doc(id).delete();
  return NextResponse.json({ success: true });
}
```

```ts
// PostEditActions.tsx — 클라이언트에서 API 호출
const res = await fetch(`/api/posts/${postId}`, { method: "DELETE" });
```

---

## 3. 레이스 캘린더 날짜 범위 표시

**파일**:
- `src/components/home/SeasonCalendar.tsx`
- `src/app/season/page.tsx`

레이스 날짜를 레이스 당일만 표시하던 것을 FP1 ~ 레이스 당일 범위로 변경.

```
변경 전: 03.15
변경 후: 03.13~15  (같은 달일 경우)
         03.29~04.01  (월이 넘어갈 경우)
```

두 파일 공통 헬퍼 함수:
```ts
function fmtDateRange(raceDate: string, fp1Iso?: string): string {
  const raceMM = raceDate.slice(5, 7);
  const raceDD = raceDate.slice(8, 10);
  const raceStr = `${raceMM}.${raceDD}`;
  if (!fp1Iso) return raceStr;

  const fp1 = new Date(fp1Iso);
  const fp1MM = String(fp1.getUTCMonth() + 1).padStart(2, "0");
  const fp1DD = String(fp1.getUTCDate()).padStart(2, "0");
  if (fp1DD === raceDD) return raceStr;

  return fp1MM === raceMM
    ? `${fp1MM}.${fp1DD}~${raceDD}`
    : `${fp1MM}.${fp1DD}~${raceMM}.${raceDD}`;
}
```

`sessions` 데이터가 없는 경우 레이스 날짜만 표시 (기존 동작 유지).

---

## 4. 레이스 결과 전체 데이터 누락 및 정렬 수정

**파일**:
- `src/lib/api/jolpica.ts`
- `src/lib/data/live.ts`

### 원인
Jolpica API 기본 반환 개수 제한으로 일부 드라이버 누락.
정렬 기준 없이 API 반환 순서 그대로 표시.

### 해결 — 데이터 누락 (limit=100 추가)
```ts
// jolpica.ts
getRaceResults: `/${season}/${round}/results?limit=100`
getQualifying:  `/${season}/${round}/qualifying?limit=100`
getSprintResults: `/${season}/${round}/sprint?limit=100`
```

### 해결 — position 정렬
```ts
// live.ts
results: race.Results.map(mapResult).sort(sortResults)
```

---

## 5. DNF/DNS 드라이버 하단 정렬

**파일**: `src/lib/data/live.ts`

완주자를 먼저, DNF / DNS / DSQ 드라이버를 마지막으로 정렬.

```ts
/** 완주자 먼저(position 오름차순), 미완주자(DNF/DNS/DSQ) 뒤로 */
function sortResults(a: RaceResult, b: RaceResult): number {
  const aFinished = !isNaN(parseInt(a.positionText));
  const bFinished = !isNaN(parseInt(b.positionText));
  if (aFinished !== bFinished) return aFinished ? -1 : 1;
  return a.position - b.position;
}
```

**판별 기준**: Jolpica `positionText` 필드
- 완주: `"1"` ~ `"20"` (숫자 파싱 가능)
- 미완주: `"R"` (Retired), `"D"` (DSQ), `"W"` (DNS) 등

적용 범위: 결승 + 스프린트 결과 모두.

---

## 6. AdminDashboardClient 타입 오류 수정 (기존 버그)

**파일**: `src/app/admin/dashboard/AdminDashboardClient.tsx`

`Props` 인터페이스에 `pendingRounds`, `driverList` 누락으로 빌드 실패하던 기존 버그 수정.

```ts
interface Props {
  initialConfig: AdminConfig;
  pendingRounds: { round: number; label: string }[];
  driverList: { id: string; firstName: string; lastName: string; teamId: string }[];
}
```

---

## 변경 파일 목록

| 파일 | 변경 내용 |
|------|-----------|
| `src/app/history/page.tsx` | Table 컴포넌트 적용 |
| `src/app/api/posts/[id]/route.ts` | DELETE 핸들러 추가 (Admin SDK) |
| `src/components/community/PostEditActions.tsx` | 삭제 방식 API 호출로 변경 |
| `src/components/home/SeasonCalendar.tsx` | 날짜 범위 표시 |
| `src/app/season/page.tsx` | 날짜 범위 표시, fmtDateRange 헬퍼 추가 |
| `src/lib/api/jolpica.ts` | limit=100 추가 (레이스/퀄리/스프린트) |
| `src/lib/data/live.ts` | sortResults 함수, 정렬 적용 |
| `src/app/admin/dashboard/AdminDashboardClient.tsx` | Props 타입 오류 수정 |
