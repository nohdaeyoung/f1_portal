# TODOS

`/plan-eng-review` (2026-08-27) 에서 확인받아 기록한 이연 항목.

---

## 1. CI 부재 — 테스트를 돌려주는 곳이 없다

**What** — 테스트를 자동 실행하는 파이프라인(GitHub Actions 등)을 만든다.

**Why** — Vitest 도입(T4)을 결정했는데, 돌려주는 주체가 없으면 안전망이 아니라 장식이 된다.

**Context** — 확인된 현황:
- `.github/` 디렉토리가 없다
- `vercel.json` 에 `"git": { "deploymentEnabled": false }` — 배포는 `vercel --prod` CLI로만 된다
- 즉 push 시점에 자동으로 돌아가는 검사가 하나도 없다

**Pros** — 테스트가 실제로 회귀를 잡기 시작한다. 리팩토링을 여러 패스로 나눌 때 매번 손으로 확인할 필요가 없다.

**Cons** — 설정 시간이 들고, 외부 API에 의존하는 테스트는 CI에서 불안정하다. 모킹 계층이 같이 필요하다.

**Depends on** — T4(Vitest 도입)가 먼저. 테스트가 없는 상태에서 CI를 먼저 깔 이유는 없다.

---

## 2. `/api/posts?round=N` 이 500을 반환한다

**What** — Firestore 복합 인덱스를 만들어 라운드 필터 쿼리를 살린다.

**Why** — 지금은 호출하는 쪽이 없어 증상이 안 보인다. 라운드별 글 목록이 필요해지는 순간 바로 터진다. 리뷰 글이 36편 쌓였으므로 그럴 일이 생길 가능성이 전보다 높다.

**Context** — 2026-08-27 확인: `https://f1.324.ing/api/posts?round=1` → 500.
원인은 `where("roundTag","==",N)` 과 `orderBy("createdAt","desc")` 를 같이 쓰면 Firestore가 복합 인덱스를 요구하기 때문이다 (`src/app/api/posts/route.ts`).
게시 스크립트 `scripts/post-review.py` 는 이 경로를 피해 `/community/{slug}` 의 200/404로 존재 확인을 하도록 이미 우회해뒀다 — 인덱스를 만들면 그 우회도 걷을 수 있다.

**Pros** — Firestore 콘솔에서 인덱스 하나 생성이면 끝. 색인 대기만 몇 분.

**Cons** — 당장 쓰는 화면이 없어 가치가 즉각적이지 않다. 인덱스는 쓰기 비용을 약간 늘린다.

**Depends on** — 없음. 독립적으로 가능.

---

## 3. 모놀리스 분해 — GP 페이지 2종

**What** — `src/app/season/race/[round]/page.tsx` (1,369줄) 와 `[session]/page.tsx` (748줄) 를 쪼개 테이블 컴포넌트와 fetch 로직을 분리한다.

**Why** — 한 파일에 OpenF1 fetch 헬퍼, 테이블 컴포넌트 7개, 세션 설정, 페이지 렌더링이 전부 들어 있다. 기능을 얹을 때마다 마찰이 커진다 (2026-08-26 리뷰 버튼 작업에서 직접 겪음).

**Context** — `/plan-eng-review` D3에서 명시적으로 연기했다. 이유는 구조 변경과 동작 변경을 한 패스에 섞지 않기 위해서다.

**Depends on** — T2(세션 탭 경로 이관), T6(헬퍼 이사) 이후.
⚠ **선행 조건**: T2가 세션 뷰 코드를 경로 라우트로 빼내면 `page.tsx` 분량이 크게 줄어들 가능성이 높다. **분해 계획을 짜기 전에 분량을 다시 측정할 것.** 지금의 2,117줄을 기준으로 계획하면 낭비가 된다.

**Pros** — 다음 작업자가 만나는 벽이 낮아진다.

**Cons** — 런타임 이득이 없다. 순수 유지보수 작업.
