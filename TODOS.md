# TODOS

`/plan-eng-review` (2026-08-27) 에서 확인받아 기록한 이연 항목.

---

## 1. CI 부재 — 테스트를 돌려주는 곳이 없다

**What** — 테스트를 자동 실행하는 파이프라인(GitHub Actions 등)을 만든다.

**Why** — Vitest 도입(T4)을 결정했는데, 돌려주는 주체가 없으면 안전망이 아니라 장식이 된다.

**Context** — 확인된 현황:
- `.github/` 디렉토리가 없다
- `vercel.json` 에 `"git": { "deploymentEnabled": false }` — 배포는 `vercel --prod` CLI로만 된다
- 즉 push 시점에 자동으로 돌아가는 검사가 하나도 없다

**Pros** — 테스트가 실제로 회귀를 잡기 시작한다. 리팩토링을 여러 패스로 나눌 때 매번 손으로 확인할 필요가 없다.

**Cons** — 설정 시간이 들고, 외부 API에 의존하는 테스트는 CI에서 불안정하다. 모킹 계층이 같이 필요하다.

**Depends on** — T4(Vitest 도입)가 먼저. 테스트가 없는 상태에서 CI를 먼저 깔 이유는 없다.

---

## 2. `/api/posts?round=N` 이 500을 반환한다

**What** — Firestore 복합 인덱스를 만들어 라운드 필터 쿼리를 살린다.

**Why** — 지금은 호출하는 쪽이 없어 증상이 안 보인다. 라운드별 글 목록이 필요해지는 순간 바로 터진다. 리뷰 글이 36편 쌓였으므로 그럴 일이 생길 가능성이 전보다 높다.

**Context** — 2026-08-27 확인: `https://f1.324.ing/api/posts?round=1` → 500.
원인은 `where("roundTag","==",N)` 과 `orderBy("createdAt","desc")` 를 같이 쓰면 Firestore가 복합 인덱스를 요구하기 때문이다 (`src/app/api/posts/route.ts`).
게시 스크립트 `scripts/post-review.py` 는 이 경로를 피해 `/community/{slug}` 의 200/404로 존재 확인을 하도록 이미 우회해뒀다 — 인덱스를 만들면 그 우회도 걷을 수 있다.

**Pros** — Firestore 콘솔에서 인덱스 하나 생성이면 끝. 색인 대기만 몇 분.

**Cons** — 당장 쓰는 화면이 없어 가치가 즉각적이지 않다. 인덱스는 쓰기 비용을 약간 늘린다.

**Depends on** — 없음. 독립적으로 가능.

---

## 3. 빌드 시 Jolpica 429 — 프리렌더가 늘면서 같이 늘었다

**What** — 빌드 중 Jolpica 429 를 줄인다. 현재 빌드 로그 기준 130건.

**Why** — 화면이 깨지지는 않는다(캘린더는 실제 정적 일정으로 폴백). 다만 폴백이
많아질수록 빌드 시점 데이터가 오래된 값으로 굳고, 429 가 더 늘면 순위표처럼
폴백이 없는 데이터는 빈 상태로 프리렌더된다.

**Context** — 2026-08-27 측정:
- 프리렌더 라우트 140 → 284 로 늘린 뒤 429 가 82 → 130 건으로 증가
- 130건 중 77건이 `[live] calendar API failed`
- 원인은 페이지마다 `fetchCalendar` 를 부르는데 빌드 워커끼리 `unstable_cache` 를
  공유하지 않는 것. 워커 수만큼 같은 요청이 중복된다
- `lib/api/http.ts` 의 429 재시도는 이미 적용돼 있고, 그것으로도 남는 양이다

**후보 접근** — 빌드 타임에 캘린더를 한 번만 읽어 모듈 스코프에 담기 / 정적
일정을 빌드 시 1차 소스로 쓰고 API 는 winner·status 갱신에만 쓰기 / 빌드 동시성 낮추기.

**Pros** — 빌드가 조용해지고 프리렌더 데이터 신선도가 올라간다.

**Cons** — 빌드 전용 경로를 만들면 런타임과 동작이 갈릴 수 있다.

**Depends on** — 없음.

---

## 4. 모놀리스 분해 — GP 페이지 2종 (범위 재측정 완료)

**상태** — 라운드 페이지는 **해결됨**. 세션 탭을 경로 라우트로 옮기면서
1,369줄 → 595줄이 됐고, 남은 코드는 개요 화면에 응집돼 있다.

**남은 것** — `[session]/page.tsx` 가 788줄이다. 다만 이건 한 세션을 렌더하는
응집된 코드고, 테이블 컴포넌트를 빼내는 정도가 전부라 우선순위가 낮다.

**판단** — 원래 이 항목의 선행 조건이었던 "T2·T6 이후 재측정"은 끝났다.
지금 상태로는 분해할 만한 게 별로 없다. 다음에 이 파일을 크게 건드릴 일이
생기면 그때 같이 하는 것으로 충분하다.

---

## 5. `SessionTabs` 가 렌더 중에 `Date.now()` 를 부른다

**상태:** 열림 (낮음) · `/qa` 2026-08-27 발견

`src/components/season/SessionTabs.tsx:45` 가 렌더 시점에 `Date.now()` 로
과거/미래 세션을 가른다. 서버 컴포넌트가 프리렌더되므로 그 판단이 빌드 시점에
굳는다. 미래 세션 탭에는 `pointer-events-none` 이 붙으므로, 세션이 시작된 뒤에도
ISR revalidate(300초) 전까지 탭이 죽어 있을 수 있다.

`npx eslint` 도 `react-hooks/purity` 로 잡는다 (`Cannot call impure function`).

**선택지**
- 과거/미래 구분을 없앤다 — 전부 같은 스타일, 전부 클릭 가능. 가장 작고 정직하다.
- 시각적 흐림만 남기고 `pointer-events-none` 을 뺀다 — lint 오류는 남는다.
- 클라이언트에서 계산한다 — 이 작은 신호에 클라이언트 번들을 다시 늘리는 건 과하다.

레포에 기존 lint 오류가 2건 더 있어 lint 정리와 묶어서 하는 편이 낫다.
